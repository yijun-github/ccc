{"_id":"_design/general","views":{"state_hourly_tweet":{"reduce":"function (keys, values, rereduce) {\n  if (rereduce) {\n    return sum(values);\n  } else {\n    return values.length;\n  }\n}","map":"function (doc) {\n  if (doc.state){\n    emit([doc.state, doc.hour], {count:1})\n  }\n}"},"language_hour":{"reduce":"function (keys, values, rereduce) {\n  if (rereduce) {\n    return sum(values);\n  } else {\n    return values.length;\n  }\n}","map":"\nfunction (doc) {\n  if (doc.language){\n    emit([doc.language, doc.hour], {count:1})\n  }\n}\n"},"suburb_hourly_tweet":{"reduce":"function (keys, values, rereduce) {\n  if (rereduce) {\n    return sum(values);\n  } else {\n    return values.length;\n  }\n}","map":"function(doc) {\n// make sure this is a \n  if (doc.state)  {\n    var locationArray = doc.location.split(\",\");\n    var suburb = locationArray[0].trim();\n  \temit({suburb: suburb, state:doc.state}, {hour:doc.hour,count: 1});\n    }\n}"},"general_sentiment":{"reduce":"function (keys, values, rereduce) {\n  if (rereduce) {\n    return sum(values);\n  } else {\n    return values.length;\n  }\n}","map":"function (doc) {\n  var sentiment = doc.sentiment;\n  var sentimentCategory;\n  if (sentiment < 0) {\n      sentimentCategory = \"Negative\";\n    } else if (sentiment === 0) {\n      sentimentCategory = \"Neutral\";\n    } else {\n      sentimentCategory = \"Positive\";\n    }\n  emit(sentimentCategory, 1);\n}"}},"language":"javascript"}
